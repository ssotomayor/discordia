[package]
name = "dioxusfun-bot"
version = "0.1.0"
edition = "2024"
description = "Async client SDK for writing dioxusfun bots (Tier 1 external WS bots)."

[dependencies]
dioxusfun-protocol = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "net", "time", "sync"] }
tokio-tungstenite = { workspace = true }
futures-util = { workspace = true }
# Nostr identity (secp256k1 / Schnorr).
secp256k1 = { workspace = true }
hex = { workspace = true }
sha2 = "0.10"
bs58 = { workspace = true }
serde_json = { workspace = true }
url = "2"
rand = "0.8"

[dev-dependencies]
# Examples use the full tokio runtime macros.
tokio = { workspace = true, features = ["full"] }
